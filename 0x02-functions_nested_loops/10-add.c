#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *
 * @x: first number
 * @y: second number
 *
 * Return: sum of x + y
*/

int add(int x, int y)
{
	int some;

	some = x + y;
	return (some);
}
